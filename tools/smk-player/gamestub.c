/* Game-side symbols the delinked AIL/Smacker code references but that we
 * don't pull in (they live in the C2 game, not the libraries).
 * show_smksum_screen shows a Smacker checksum/debug screen; a no-op is
 * fine for the player. */
void show_smksum_screen(void)
{
}
